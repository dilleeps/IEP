# Local socket connections: allow container-internal tooling
local   all             all                                     trust

# Require SSL + client certificate + password for all TCP connections
hostssl all             all             0.0.0.0/0               scram-sha-256 clientcert=verify-full
hostssl all             all             ::/0                    scram-sha-256 clientcert=verify-full
